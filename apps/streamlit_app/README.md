# Streamlit UI (WeatherAI MCP)

Lightweight UI to call the MCP server and render plots.  
Narrative/LLM layer is optional and not required for functionality.

## Dev
```bash
pip install -r requirements.txt
streamlit run app.py
```
### `apps/streamlit_app/tests/test_ui.py`
```python
def test_placeholder():
    assert True
```